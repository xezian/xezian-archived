#endpoint: ${env:PRISMA_ENDPOINT}
#endpoint: https://xezian-f41b40525e.herokuapp.com/server/prod
endpoint: http://localhost:4466
datamodel: datamodel.prisma
secret: ${env:PRISMA_SECRET}
hooks:
  post-deploy:
    - graphql get-schema -p prisma